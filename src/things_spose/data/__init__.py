"""Stage 1 — loading, assembling, and sampling the raw data.

``dataio`` assembles the full 1,854-object dataset the analyses read (the port of
the MATLAB preamble); ``sampling`` handles the raw odd-one-out triplets that feed
training. Import submodules directly::

    from things_spose.data import dataio, sampling
"""
